<header>
  {{#if connecting}}
  <!-- Connecting... -->
  {{else}}
    {{#unless connected}}
      Not connected.
    {{/unless}}
  {{/if}}
</header>

{{#if currentListing}}
  {{directory-listing items=currentListing}}
{{/if}}