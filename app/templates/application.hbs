<div class="ember-view">
<div id="app-container" class={{this.connectedClass}}>
  <aside>
    {{#if this.connected}}
      <AccountInfo @userAddress={{this.userAddress}} />
      <CategoriesNav @categories={{this.categories}} />
    {{/if}}
  </aside>
  <main>
    {{outlet}}
  </main>
</div>
</div>
